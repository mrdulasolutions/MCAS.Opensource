# Colab GPU environment for REINVENT 4 / DiffDock / heavy generative + docking work.
# Install in a Colab notebook with:
#   !pip install -r requirements-colab.txt

# Core cheminformatics
rdkit>=2023.9
pandas>=2.0
numpy>=1.24
requests>=2.31
tqdm>=4.66
matplotlib>=3.7

# QSAR + ML
PyTDC>=0.4
deepchem>=2.7
scikit-learn>=1.3

# Generative design (REINVENT 4 is installed via its own pip package or git clone in the notebook;
# pinning torch + CUDA-compatible deps here so the env is reproducible).
torch>=2.1
torchvision
torchaudio

# Optional: DiffDock for blind docking (installed via git clone inside the notebook)
biopython>=1.81
prody>=2.4
mdanalysis>=2.5

# Structure download
gemmi>=0.6
